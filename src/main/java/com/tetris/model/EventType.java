package com.tetris.model;

/**
 * Types of movement events.
 */
public enum EventType {
    /** Move down. */
    DOWN,
    /** Move left. */
    LEFT,
    /** Move right. */
    RIGHT,
    /** Rotate. */
    ROTATE,
    /** Hold brick. */
    HOLD
}
